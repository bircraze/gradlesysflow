def subPrjRoot = "subprojects"
def subPrjs = []

new File(subPrjRoot).eachDir() {
    subPrjs << it.toString()
}

subPrjs.each {
    def subPrjName = it.substring(subPrjRoot.length() + 1)
    include subPrjName
    project(":${subPrjName}").projectDir = new File("${it}")
}
